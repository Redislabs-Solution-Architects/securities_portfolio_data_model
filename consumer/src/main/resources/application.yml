spring:
  redis:
    host: localhost
    port: 6379
    password: admin
  main:
    allow-circular-references: true
    
price:
  update:
    stream: price_update_stream

logging:
  level:
    org:
      springframework:
        web: DEBUG